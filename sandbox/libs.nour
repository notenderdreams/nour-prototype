#include "nour.h"

Library math_utils = {
    .type = STATIC,
    .sources = {
        "math/*.c",
    },
    .includes = { "math" },
};

Library math_utils_shared = {
    .type = SHARED,
    .sources = {
        "math/*.c",
    },
    .includes = { "math" },
};
