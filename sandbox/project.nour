#include "nour.h"

Project sandbox_app = {
    .version = "0.1.0",
    .sources = {
        "sandbox/*.c",
        "sandbox/math/*.c",
        "sandbox/utils/*.c"
    },
    .cc = "gcc",
    .build_dir = "build",
    .optimize = OPT_DEBUG,
    .warnings = WARN_ALL | WARN_EXTRA,
    .sanitizers = SAN_NONE,
    .cflags = { "-Isandbox" }
};

