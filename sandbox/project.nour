#include "nour.h"

Project sandbox_app = {
    .version = "0.1.0",
    .sources = {
        "sandbox/*.c",
        "sandbox/math/*.c",
        "sandbox/utils/*.c"
    },
    .cc = "gcc",
    .build_dir = "build",
    .cflags = { "-Wall", "-Wextra", "-Isandbox" }
};

