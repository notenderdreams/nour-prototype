#include "nour.h"

Project sandbox_app = {
    .version = "0.1.0",
    .sources = (char*[]){"sandbox/*.c", NULL},
    .cc = "gcc",
    .build_dir = "build",
    .cflags = (char*[]){ "-Wall", "-Wextra", NULL }
};

